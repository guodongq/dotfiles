- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.gitconfig: gitconfig
    ~/.config:
      force: true
      path: config
    ~/.zprofile: zprofile
