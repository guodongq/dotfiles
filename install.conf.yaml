- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.gitconfig: 
      force: true
      path: git/gitconfig
    ~/.config/nvim:
      force: true
      path: config/nvim
    ~/.zprofile: zprofile
