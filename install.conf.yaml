- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.zshrc: .zshrc
    ~/.vimrc: .vimrc
    ~/.npmrc: .npmrc
    ~/.gitconfig:
      force: true
      path: .gitconfig
    ~/.config/nvim:
      force: true
      path: nvim
