- defaults:
    link:
      create: true
      relink: true

- link:
    # zsh
    ~/.zshrc: .zshrc
    # git
    ~/.gitconfig:
      force: true
      path: .gitconfig
    # nvim
    ~/.config/nvim:
      force: true
      path: .config/nvim
    # npmrc
    ~/.npmrc: .npmrc
